add prettier and eslint

disable the rule that does renaming App to AppComponent and app.ts to app.component.ts, app.ts is better thant app.component.ts

create a folder for pages on src\app and add landing, buildings, viewer, contact pages.

update router and app component to display a layout with tabs to access different pages.
---